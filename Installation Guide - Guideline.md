## Installation Steps

Based on the provided information, I will install the required dependencies by running <code>npm install</code>. Then, I will configure the environment variables using the <code>export</code> command. Finally, I will run the application using the <code>node</code> command.
